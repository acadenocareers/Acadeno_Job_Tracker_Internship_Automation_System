name: Kerala IT Park Job Scraper Automation

on:
  schedule:
    - cron: '30 2,15 * * *'   # 8:00 AM & 9:00 PM IST
    - cron: '50 10 * * *'     # 4:20 PM IST
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install selenium requests beautifulsoup4 reportlab webdriver-manager

      - name: Run Maitexa job scraper
        run: python app.py

      - name: Upload PDF temporarily to transfer.sh
        id: upload
        run: |
          echo "Uploading Maitexa_Green_Adjusted_Brochure.pdf to transfer.sh..."
          url=$(curl -s --upload-file Maitexa_Green_Adjusted_Brochure.pdf https://transfer.sh/Maitexa_Green_Adjusted_Brochure.pdf)
          echo "pdf_url=$url" >> $GITHUB_ENV
          echo "✅ Uploaded successfully: $url"

      - name: Send professional email with proper view/download buttons
        env:
          EMAIL_USER: ${{ secrets.EMAIL_USER }}
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          PDF_URL: ${{ env.pdf_url }}
        run: |
          python - <<'EOF'
          import os, smtplib
          from email.mime.text import MIMEText
          from email.mime.multipart import MIMEMultipart
          from datetime import datetime

          sender = os.getenv("EMAIL_USER")
          password = os.getenv("EMAIL_PASS")
          recipients = [x.strip() for x in os.getenv("EMAIL_TO", "").split(",") if x.strip()]
          pdf_url = os.getenv("PDF_URL")

          msg = MIMEMultipart("alternative")
          msg["From"] = f"Maitexa Technologies <{sender}>"
          msg["To"] = ", ".join(recipients)
          msg["Subject"] = f"🌟 Exciting Career Opportunities Await You – {datetime.now().strftime('%d %b %Y')}"

          html_content = f"""
          <html>
          <body style="font-family: Arial, sans-serif; font-size:15px; color:#222; line-height:1.6;">
              <p>Dear Team,</p>

              <p>We are thrilled to inform you that <strong>new and exciting career opportunities</strong> 
              are now open for talented individuals like you.</p>

              <p>At <strong>Maitexa Technologies</strong>, we believe that dreams are the starting point of every great success story. 
              This is your chance to chase your dreams and build a rewarding career with us.</p>

              <p>✨ <strong>Explore the latest openings</strong> and find the role that best fits your passion and skills.</p>

              <div style="text-align: center; margin-top: 20px;">
                  <a href="{pdf_url}" target="_blank"
                     style="background-color:#007A33; color:#fff; padding:12px 25px; 
                     text-decoration:none; border-radius:6px; font-weight:bold; margin-right:10px;">
                     👀 View Current Openings
                  </a>

                  <a href="{pdf_url}" download
                     style="background-color:#007A33; color:#fff; padding:12px 25px; 
                     text-decoration:none; border-radius:6px; font-weight:bold;">
                     ⬇️ Download Brochure (PDF)
                  </a>
              </div>

              <p style="margin-top: 20px;">
              Take a moment to look through the opportunities and apply for the position that aligns with your goals.<br>
              Your next big move could start today!
              </p>

              <p>We wish you all the best in your career journey. Let’s build a brighter future together!</p>

              <p style="margin-top:20px;">Warm regards,<br>
              <strong style="color:#007A33;">Maitexa Technologies Pvt Ltd</strong><br>
              Kadannamanna, Malappuram, Kerala – 679324<br>
              <a href='mailto:contact@maitexa.com'>contact@maitexa.com</a> |
              <a href='https://www.maitexa.com'>www.maitexa.com</a>
              </p>

              <p style="font-size:12px; color:#666;">— Generated automatically by Maitexa Job Tracker © 2025</p>
          </body>
          </html>
          """

          msg.attach(MIMEText(html_content, "html"))

          with smtplib.SMTP("smtp.gmail.com", 587) as server:
              server.starttls()
              server.login(sender, password)
              server.send_message(msg)

          print(f"✅ Email sent with in-browser PDF view link: {pdf_url}")
          EOF
